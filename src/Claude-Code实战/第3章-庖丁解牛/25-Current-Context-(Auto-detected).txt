维度               不用 !`command`         用 !`command`
─────────────────────────────────────────────────────────
Claude 启动时上下文  空白，需多轮探索        已注入关键信息
首次响应工具调用数   3-5 次（收集信息）      0-1 次（直接行动）
Token 消耗          高                     低
响应速度            慢                     快
结果一致性          低（可能遗漏信息）       高（固定注入相同信息）
