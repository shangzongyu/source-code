<!-- .claude/rules/database.md -->
---
paths:
  - "prisma/**"
  - "src/repositories/**"
---

# 数据库规范

- 迁移文件一旦提交到 main 分支就不允许修改，只能创建新迁移
- 所有查询必须通过 repository 层，service 层禁止直接调用 Prisma
- 批量操作使用事务包裹，超过 100 条记录的写入必须分批
