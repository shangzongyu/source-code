{
  "name": "good-day",
  "version": "1.0.0",
  "description": "A simple greeter that knows morning from night time",
  "main": "build/greeting.js",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^5.7.0",
    "typescript": "^2.5.2"
  }
}
