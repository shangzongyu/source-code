# AGETNS.md

## 项目概览

这是一个书籍和教程源码整理仓库：

- `README.md`：主索引，按主题列出书籍/教程源码和封面图。
- `src/`：各书籍、教程、示例项目的源码目录，多语言混合。
- `images/`：README 中引用的封面图片。
- `scripts/`：少量媒体压缩脚本。

## 给代理的工作规则

1. **不要把仓库当成单一应用。** 这里没有统一的 build/test/lint 命令；每个 `src/*` 目录可能是独立项目。
2. **优先保护第三方源码。** `src/` 下大多是书籍/教程原始代码；除非用户明确要求修某一本书/某个目录，不要批量格式化、重命名或“现代化”这些代码。
3. **改索引要保持路径可点击。** 更新 `README.md` 时，确认链接目录和 `images/` 图片文件真实存在，中文、空格、标点路径要保持原样。
4. **少改文件。** 通常新增条目只需要改 `README.md`，新增封面只放 `images/`，新增源码只放对应 `src/<name>/`。
5. **不要引入全局依赖。** 除非用户明确要维护脚本，不要添加 package manager、CI、格式化器或根级构建配置。
6. **大文件谨慎处理。** 图片、压缩包、书籍源码可能很大；避免无意义读取/重写。需要统计或批量检查时用脚本按文件名/路径处理。

## 常用检查

```bash
# 查看 README 中引用但可能缺失的本地链接/图片（人工复核输出）
rg '\]\(\./|src=\"\./' README.md

# 查看新增文件
find src images scripts -maxdepth 2 -type f | sort
```

## 目录命名约定

- `src/` 下目录名尽量沿用书名、官方仓库名或 README 已有风格。
- `images/` 下封面名尽量和书名一致，避免无意义重命名已有图片。
- 中文书名、英文书名和原始仓库名可以共存；不要为了统一风格破坏已有链接。

## 提交前最小自检

- `git status --short`：确认只改了本次任务需要的文件。
- 如果改了 `README.md`：检查新增 Markdown 链接、图片路径是否存在。
- 如果改了 `scripts/`：至少用一个小样本跑一次对应脚本。
